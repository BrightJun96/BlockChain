import * as React from "react";
import BlockChain from "../components/BlockChain";

export interface IContainerBlockChainProps {}

export default function ContainerBlockChain(props: IContainerBlockChainProps) {
  return <BlockChain />;
}
